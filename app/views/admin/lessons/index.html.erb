<% provide(:title, "授業") %>
<div class="container">
    <div class="row">
        <div class="col-sm-8 px-sm-0 offset-sm-1">
            <h2 class="my-5">授業一覧</h2>
        </div>
        <div class="col-sm-2 px-sm-0 text-right mt-5">
            <%= link_to new_admin_lesson_path,class:"btn btn btn-outline-secondary" do %>
            <i class="fas fa-plus fa-2x "></i>
            <% end %>
        </div>
    </div>
    <div class="row">
    　<div class="col-sm-10 offset-1">
        <%= render 'index', lessons: @lessons%>
          <%= paginate @lessons %>
        </div>
    </div>
</div>
