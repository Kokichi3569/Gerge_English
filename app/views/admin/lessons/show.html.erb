<% provide(:title, "授業詳細") %>
<div class="container">
  <h2 class="mt-5 text-center">授業詳細</h2>
    <div class="row align-items-center m-5">
      <div class="col-lg text-center mb-5">
        <%= attachment_image_tag @lesson, :image, :size => '300x250'%>
      </div>
      <div class="col-lg">
          <table class="table">
            <tr>
              <th>商品名</th>
              <td><%= @lesson.name %></td>
            </tr>
            <tr>
              <th>商品説明</th>
              <td><%= @lesson.introduction %></td>
            </tr>
            <tr>
              <th>ジャンル</th>
              <td><%= @lesson.genre.name %></td>
            </tr>
            <tr>
              <th>
                  税込価格<br>
              </th>
              <td><%= @lesson.price %> (<%= @lesson.price %>) 円</td>
            </tr>
            <tr>
              <th>販売ステータス</th>
                <td>
                     <% if @lesson.is_active == true %>
                        <p class="text-success ">販売中</p>
                     <% else %>
                        <p class="text-secondary ">販売停止中</p>
                     <% end %>
                </td>
            </tr>
          </table>
          <div class="d-flex justify-content-around">
            <p><%= link_to '編集する', edit_admin_item_path(@lesson.id), class: 'btn btn-success' %></p>
            <p><%= link_to "商品一覧へ戻る", admin_lessons_path, class:"btn btn-outline-secondary"%></p>
          </div>
   　　 </div>
  　</div>
 </div>

